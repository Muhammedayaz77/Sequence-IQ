Sequence IQ — Screen-by-Screen Graphics Package

12 finalized screen manifests + one shared library of unique production assets.
No identical image is repeated for different screens.
SVG is used as the native scalable production format.
When a screen is revised, its asset set can be regenerated while keeping the screen design and graphics package synchronized.